{
  "devDependencies": {
    "jasmine": "^4.1.0"
  },
  "scripts": {
    "autodoc-gibberish": "npx documentation build { src/gibberish.js } -f html -o docs/gibberish",
    "autodoc-singleton": "npx documentation build { src/singleton.js } -f html -o docs/singleton",
    "autodoc-sun": "npx documentation build { src/sun.js } -f html -o docs/sun",
    "test": "jasmine"
  },
  "type": "module",
  "dependencies": {
    "documentation": "^14.0.0"
  }
}
